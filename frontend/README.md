# Frontend

Vite React UI for the associative chat prototype.

## Run

```bash
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173` on this machine, or `http://chunky.local:5173` from another
device on the local network.

The Vite dev server proxies `/api` to the Flask backend at `http://localhost:5000`.
